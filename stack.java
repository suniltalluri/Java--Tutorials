public class stack { 
    int arr[];
    int capacity;
    int top;
    // creating a stack:
    stack(int size){
        arr=new int [size];
        capacity=size;
        top =-1;
         }
    //adding elements in stack
        void push(int x){
            if(is full()){
             System.out.println("retrun 1");
                else 
               {
                   top++;
                   stack()=11;
                   System.out.println("push element:" + element);
               }
            }
        }
         
    }
